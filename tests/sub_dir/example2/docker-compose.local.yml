version: '3.8'

services:
  example2_1-local:
    image: hello-world:latest
    restart: unless-stopped
    container_name: example2_1-local
    environment:
      - FOO=${FOO}

  example2_2-local:
    image: hello-world:latest
    restart: unless-stopped
    container_name: example2_2-local